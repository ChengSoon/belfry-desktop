/* 图标尺寸。lucide 的 size 是 JS 数字，吃不到 CSS 变量，所以字号能收进
   styles.css 的 --fs-*，图标只能在这里单独定一份。

   两者是配套的：图标要比同行文字大 2px 左右才压得住，改字号档位时这里也要跟着动。 */
export const ICON = {
  /** 配 --fs-xs：分组折叠箭头、行内关闭 */
  xs: 14,
  /** 配 --fs-sm：菜单项、列表项前缀 */
  sm: 15,
  /** 配 --fs-md：工具栏、面板标题 */
  md: 16,
  /** 独立强调：主题切换、告警 */
  lg: 17,
} as const;
