import type { PanelWidthSpec } from "../panel/panelWidth";

export const FILE_PREVIEW_WIDTH: PanelWidthSpec = {
  compactWidth: 360,
  defaultWidth: 520,
  edge: "right",
  key: "belfry.file-preview-width.v1",
  max: 760,
  min: 320,
  step: 12,
};
