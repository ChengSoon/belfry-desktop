# CM-16 命令与 Prompt 收藏插件

目标：以独立、可选 PI 插件提供全局/当前项目收藏、分类搜索、变量预览与编辑后复制。
沿用 frontend-design：两栏轻量布局，项目与数量为次级信息，编辑和预览按需出现。

## 设计

- 插件位于 `examples/plugins/command-library`，提供可安装 `.piplug`，不增加常驻核心入口。
- 权限仅面板与写剪贴板。使用宿主 workspace 上下文和插件自身数据目录；不请求终端执行、网络或凭据权限。
- 全局模板可复用，项目模板按宿主路径隔离；后端再次验证作用范围，面板不能伪造其他项目。
- 变量支持项目名/原始路径与显式 POSIX/PowerShell 引用。预览可编辑，复制前再核对项目上下文。
- 版本化、有界存档，保存前比较版本摘要，原子写入；归档可恢复，损坏文件不覆盖。
- 没有自动投递或执行。插件禁用后核心工作台维持原有行为。

## 进度

- [x] 模型、跨项目边界、变量和保存冲突的测试与实现。
- [x] 面板、编辑预览、复制及主题/键盘适配。
- [x] 宿主集成、无警告校验打包；4 项模型/真实 Node 测试通过。
- [x] 桌面安装、真实复制、跨项目隔离及中文空格路径预览。
- [x] 暗色主题、重启保存及停用验收。
- [x] 更新实施记录和小清新待办并重开回读。

3 项模型/持久化测试及 1 项真实 Node PI 宿主集成通过；校验和 `.piplug` 打包通过。
2026-09-13 已在独立 Controls QA 通过系统文件窗口安装，权限仅显示面板和写剪贴板；正式 Belfry 没有安装该测试插件。
项目 A 的“QA 项目专用”不出现在项目 B“命令 空间”，全局条目跨项目可见；POSIX 路径变量保持中文/空格并正确引用。
编辑复制预览后点击复制，再清空预览并用系统粘贴回读，结果与编辑文本完全相同；没有执行模板内容。
2026-09-14 桌面已恢复。Controls QA 新进程中插件仍安装；原项目的“QA 全局收藏”和“QA 项目专用”均保留，专属正文与复制验收文本一致，暗色面板检查通过。
在插件“生效范围”中选择“关闭”后，列表显示“已关闭”，打开面板按钮禁用，插件启动器显示没有可打开面板的插件。
核心 Shell 经系统粘贴执行固定验收命令，正常输出 `CM16_DISABLE_OK`。
正式小清新待办已更新验收详情并勾选 CM-16；关闭重开后已完成筛选保留该条，计数为“剩余 7 项 · 共 16 项”。

安装包：`examples/plugins/command-library/dist/belfry.command-library-0.1.0.piplug`。
SHA256：`e896282989780f87ec484db17c8c05cad46db6d4dac5de81e5cdd7bc6d3a409c`。
