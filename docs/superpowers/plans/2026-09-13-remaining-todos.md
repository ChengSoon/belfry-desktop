# 剩余功能继续执行

执行来源：`docs/cli-manager-feature-backlog.md`、`docs/cli-manager-implementation.md`；用户要求继续完成剩余待办。

目标：逐项交付剩余 CM 功能，完成验证后同步小清新待办；应用内控件统一自绘，界面保持简洁。

## 高层进度

- [ ] 收尾 CM-01/05 的界面验收。
- [x] 收尾设置页 UI：八个分区填满右侧可用宽度，统一页头与快捷键分组，完成窄窗口/亮暗主题及编辑弹窗的浏览器验收；原生 WebView 验收范围见实施记录。
- [x] 修复插件独立窗口的按钮/拖拽事件冲突：新增真实鼠标回归，面板与桥接 15 项、Rust 嵌入运行时 3 项通过；Windows 新包与原生窗口仍待实机复验，详见 `docs/plugins/pi-desktop-port.md`。
- [x] CM-12 焦点问题已修复，重启/迁移/目录修复均已验收，正式待办已勾选并关闭重开回读。
- [x] SSH 主机、可选 Prompt 插件（CM-11/16）：保存、隔离、真实复制、重启回读及停用验收完成；真实待办已勾选并关闭重开回读。
- [ ] 选择性备份恢复（CM-13）：桌面选域恢复/撤回、新配置导入、未选域不变和错误包提示均通过；正式详情已保存，待截图恢复后勾选并重开回读。
- [ ] Worktree 创建与收尾（CM-07/08）：实现、临时仓库测试及后台会话占用整合完成，待桌面验收。
- [ ] PTY 后台保活（CM-06）：自动验证与 macOS GUI 退出重开、同 PID/计数/单次启动及显式回收通过，待 Windows 平台验证。
- [x] 完成最终整合检查与最新 QA 构建，更新实施记录。
- [x] 同步全部 16 项待办文字进度，勾选 CM-14；关闭重开后回读 6 项完成、10 项待验收及各项详情。
- [ ] 进行中：CM-07/08 桌面 Worktree 验收；已创建任务 A，修复非法分支错误提示，9 项 Worktree 回归通过。正式待办已完成 10 项、剩余 6 项。

## 边界与执行

- 当前分支串行整合；用户已要求先本地提交当前代码，包含现有改动及 Windows 安装修复。提交后继续剩余验收，不推送或改写既有历史。
- 新能力按原待办的最小范围设计；不自动执行项目脚本，不保存明文凭据，不操作用户 Git 历史或清理其工作树。
- 有行为变化的共享模块先补关键路径测试，再实现；已有测试通过后只针对新变化扩大验证。
- 每项验证实现、类型/构建、相关回归及真实界面；未验证的平台和受阻步骤明确保留为待验收。
- 开发实例与正式 Belfry 保持运行；测试使用独立 QA 实例及本任务临时数据。
- 插件工具曾返回 `SESSION_MISMATCH`；完整应用路径已恢复原生界面，使用正式 UI 同步待办，不直接改写插件私有存档。

## 风险

CM-11/16 与 CM-12 桌面验收、待办勾选与关闭重开回读均完成。CM-13 验收通过后截图再次空白，随后报 ScreenCaptureKit -3811 / cgWindowNotFound；其完成勾选暂未执行。后台保活的 Windows 验证缺少 SDK，仍保持未验证。

CM-14 已完成目录移动后的重新选择、分组保留及亮暗/窄窗口验收，现已成功勾选并关闭重开回读。
正式待办共 16 项，已完成 CM-02/03/04/09/10/11/12/14/15/16，剩余 6 项；所有条目已同步进度、验收摘要或待验收步骤。
CM-09 分页栏和 CM-11 弹窗底栏修复构建成功（`/tmp/belfry-remaining-layout-qa.log`），仅重启了本任务 Controls QA。

最新前端全量 136 文件、772 项通过；Rust 本轮 Worktree 9 项通过，此前全量 510 项通过、8 忽略；TypeScript 工程检查、设置 UI 生产构建和此前含错误提示修复的 QA 构建通过。
新增真实 Claude/Codex 经 daemon 启动及缓存回放验证、SSE 首条事件即时转发与 UI 重新绑定验证。
插件仍报 SESSION_MISMATCH；CUA 改用 `/Applications/Belfry.app` 后成功完成待办保存和重开回读。Windows 交叉检查缺少 SDK 的 assert.h，未通过。
最终 QA 首次构建因磁盘空间不足失败，清理本项目可重新生成的旧 Rust 增量缓存后已成功重建。
GUI 保活验收中 QA 36385 已真实退出，重开 PID 为 66137；worker PID 64937 不变、计数继续且启动一次，之后已通过 UI 显式结束。后续只重启本任务独立 QA，不退出正式 Belfry。
CM-01 两家 CLI 的 UI 移除/重装保留原 Hook 已通过；Codex 信任审阅和会话统计仍待验收。
CM-01/11/16 新增桌面记录均已补入正式待办；CM-06“待 Windows 验证”文字已关闭重开核对。CM-11 和 CM-16 已勾选并重开回读。
2026-09-14 本地提交 `32fd65b` 已核对；随后累计的设置布局、插件窗控、终端焦点、主题恢复、Worktree 提示修复及验收记录，按用户最新要求纳入本轮本地提交。
未勾选项为 CM-01/05/06/07/08/13；完整证据与桌面接续清单见实施记录。
